info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: "{{basePath}}/trios/api{{masterDataBrokersPath}}"
  body:
    type: json
    data: |-
      {
        "sampleField": "{{$randomBankAccount}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
