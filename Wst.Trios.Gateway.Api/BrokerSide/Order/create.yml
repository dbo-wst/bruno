info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: "{{basePath}}/trios/gateway/api/brokerside/orders"
  body:
    type: json
    data: |-
      {
        "sampleField": "asdfasdasdff"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
